# Shared package

This directory is reserved for shared contracts, schema types, and cross-stack models
that will be used by the frontend and backend as the runtime evolves. Phase 1 keeps
this area intentionally empty and ready for future infrastructure contracts.
